
const ReviewSection = ({ info }) => {

    const {
        name,
        image,
        category,
        price,
        mealType,
    } = info;

    return (
        <div>
            
        </div>
    );
};

export default ReviewSection;